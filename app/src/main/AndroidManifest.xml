<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.osamaawad.graduationproject_tawasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TypeOfFoodPage"></activity>
        <activity android:name=".ProfilePage" />
        <activity android:name=".MealReservationPage" />
        <activity android:name=".CartPage" />
        <activity android:name=".RestaurantPage" />
        <activity android:name=".RestaurantMenuPage" />
        <activity android:name=".MainPage" />
        <activity
            android:name=".Sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Sign_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StartPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>